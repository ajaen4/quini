package badges

import (
    "fmt"
)

templ TotalMetric(name string, number float32) {
    <div class="inline-flex items-center bg-gray-700 rounded-full px-2 py-1 text-xs font-medium text-white">
        <span class="mr-2">{ name + ":" }</span>
        <span class="font-bold">{ fmt.Sprintf("%.2f€", number) }</span>
    </div>
}
